export interface UserAuthFormat {
    uid: number;
    public_id: number;
    db_id: number;
    auth_code: string;
    last_login_date: string;
    last_update_date: string;
    data_version: number;
}